{
  "_comment": "Keys = env var names in the pod. Values = GitHub repo secret names (no real values here). Create each with: gh secret set <value> --repo nurixlabs/<your-repo> --body '<actual-value>'. Delete keys you don't need.",
  "DATABASE_URL": "${secrets.DATABASE_URL}",
  "JWT_SECRET": "${secrets.JWT_SECRET}",
  "REDIS_URL": "${secrets.REDIS_URL}",
  "OPENAI_API_KEY": "${secrets.OPENAI_API_KEY}"
}