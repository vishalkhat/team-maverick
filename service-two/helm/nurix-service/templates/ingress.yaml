{{- if .Values.ingress.enabled }}
{{- $fullName := include "nurix-service.serviceName" . -}}
{{- $svcPort := .Values.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "nurix-service.fullname" . }}
  namespace: {{ .Values.namespace.name | default .Release.Namespace }}
  labels:
    {{- include "nurix-service.labels" . | nindent 4 }}
  annotations:
{{- if not .Values.ingress.ingressClassName }}
    kubernetes.io/ingress.class: "alb"
{{- end }}
    alb.ingress.kubernetes.io/group.name: "{{ .Values.global.regionCode }}-{{ .Values.global.environment }}-eks-alb-ext"
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: "{{ .Values.ingress.healthcheckIntervalSeconds }}"
    alb.ingress.kubernetes.io/healthcheck-path: "{{ .Values.ingress.healthcheckPath }}"
    alb.ingress.kubernetes.io/healthcheck-timeout-seconds: "{{ .Values.ingress.healthcheckTimeoutSeconds }}"
    alb.ingress.kubernetes.io/healthy-threshold-count: "{{ .Values.ingress.healthyThresholdCount }}"
    alb.ingress.kubernetes.io/listen-ports: {{ .Values.ingress.listenPorts | quote }}
    alb.ingress.kubernetes.io/load-balancer-name: "{{ .Values.global.regionCode }}-{{ .Values.global.environment }}-eks-alb-ext"
    alb.ingress.kubernetes.io/scheme: "{{ .Values.ingress.scheme }}"
    alb.ingress.kubernetes.io/target-type: "{{ .Values.ingress.targetType }}"
    alb.ingress.kubernetes.io/unhealthy-threshold-count: "{{ .Values.ingress.unhealthyThresholdCount }}"
  {{- with .Values.ingress.additionalAnnotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.ingressClassName }}
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if .pathType }}
            pathType: {{ .pathType }}
            {{- else }}
            pathType: Prefix
            {{- end }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
